Intallation:

    git clone git://github.com/alexhoang/dotvim.git

Create symlinks:

    ln -s ~/dotfiles/vimrc ~./vimrc

Switch to '~/.vim' directory and fetch submodules:

    cd ~/.vim
    git submodule init
    git submodule update

